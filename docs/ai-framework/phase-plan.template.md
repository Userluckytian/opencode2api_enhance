# 阶段 X：<一句话目标>

> **状态：** 计划已就绪 / 实施中 / 已完成  
> **For agentic workers:** 按 Task 顺序执行；每 Task 测完再进下一 Task。  
> **交接提示词**见文末「给接手 AI 的完整提示词」。  
> **元规范:** `docs/ai-framework/phased-plan-driven.md`

**Goal:** <一句话>  
**Architecture:** <2–3 句>  
**Tech Stack:** <关键技术>

---

## 前置阅读（必须）

| 优先级 | 文件 |
|--------|------|
| P0 | `docs/ai-framework/phased-plan-driven.md` |
| P0 | 本文件 |
| P0 | 项目进度/总控文档（若有） |
| P1 | `AGENTS.md`、`CODE_REVIEW.md` |

**仓库路径：** <绝对或相对根>  
**基线分支：** 从 `<main 或上阶段 tip>` 拉 `feat/phase-x-<slug>`

---

## Global Constraints（冲突时以本节为准）

1. …  
2. 密钥/凭证不进 git  
3. **明确不做（本阶段）**  
   - ❌ …  
4. **Git：** 小步 commit；**默认不 push**，除非人类明确要求  
5. **YAGNI：** …

---

## 阶段开头：上阶段遗留（必填小节——上一阶段验证不通过项）

> 规则：把上一阶段验收表中 ⬜/❌ 项原文抄入（**含测试/验证/代码审查任一不通过项**），标注未通过原因与来源；**本阶段优先修复并回归，全部通过后才允许推进本阶段新功能**。若无遗留写「无」。

| 上阶段遗留项 | 来源（测试/验证/审查） | 未通过原因 | 状态 |
|-------------|----------------------|-----------|------|
| （抄上一阶段验收表 ⬜/❌ 项） | | | ⬜ 待修复 |

---

## 与前后阶段

| 阶段 | 状态 | 交付 |
|------|------|------|
| 上阶段 | ✅/⬜ | … |
| **本阶段** | ⬜ | … |
| 下阶段 | | …（勿塞进本阶段） |

---

## File Structure（预期变更）

| 文件 | 动作 | 职责 |
|------|------|------|
| `path/to/file` | 新建/修改 | … |

---

## 配置或 API 契约（如有）

```json
{}
```

---

## Task 1：<标题>

**Files:** …

**行为:** …

**Steps:**

1. …  
2. 跑：`<test or build command>`  
   期望：…  
3. Commit：`<gitmoji><type>(scope): 中文说明>`

---

## Task 2：…

---

---

## 代码审查（阶段级强制环节，验收前）

**审查方：** <独立角色，非本阶段实现者>

**审查面**（按项目补）：代码风格 / 测试完整性 / 依赖合理性 / 架构红线 / 安全（密钥、注入、越权）/ API 契约一致性

| 审查项 | 结论（✅/⚠️/❌） | 问题清单 |
|--------|------------------|----------|
| 风格 | | |
| 测试完整性 | | |
| 依赖与架构红线 | | |
| 安全 | | |
| API 契约 | | |

**结论：** ✅ 通过 / ⚠️ 有条件通过（问题进验收表，❌ 下放下阶段）/ ❌ 不通过（阻塞）

---

## 验收标准总表

| # | 标准 | 通过条件 | 验证责任人 |
|---|------|----------|
| 1 | … | … | 自动化（执行方）/ 端到端（需部署机/用户） |
| 2 | 单测/构建 | 计划指定命令 exit 0 | 自动化（执行方） |
| 2b | 代码审查 | 审查结论 ✅ 或 ⚠️（问题已登记）；❌ 不通过则下放下阶段 | 独立角色（非实现者） |
| 3 | 红线 | 无禁止项；密钥未入库 |
| 4 | 密钥 | `git ls-files` 无敏感文件 | 自动化（执行方） |

---

## 风险与降级

| 风险 | 缓解 |
|------|------|
| … | … |

---

## 给接手 AI 的完整提示词

将下面整段粘贴给执行 AI 即可开工：

---

你是负责 **opencode2api_enhance** 的实现代理。请**完整执行本阶段**，不要只写方案。

### 基线
- 目录：<repo>
- 从 `<base>` 创建并切换：`feat/phase-x-<slug>`
- 已完成：…（不要重做）
- 唯一实施计划：`docs/…/plans/<this-file>.md`
- 必读：`docs/ai-framework/phased-plan-driven.md`、`AGENTS.md`

### 做
1. …  
2. …  

### 不做
- …  
- 提交密钥；未授权的 `git push`

### 工作方式
1. 先跑基线测试/构建确认干净。  
2. **严格按计划 Task 顺序**；每 Task 测试后 commit。  
3. 证据优先：完成前必须重跑计划中的验证命令。  
4. 用简体中文回复进度；代码标识符保持原样。  

### 交卷
全部完成后给出：分支名、提交列表、验收表自评、测试/构建结果、残留风险。

现在开始：读完本阶段计划，从 Task 1 执行到最后。

---

## 残留手工验收清单

（自动化之外的 GUI / 真机项）

1. …  

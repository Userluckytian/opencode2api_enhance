# 阶段计划目录（opencode2api_enhance）

将各阶段实施计划放在此目录：

```
YYYY-MM-DD-phase-<slug>.md
```

- 元规范：`../phased-plan-driven.md`
- 空白模板：`../phase-plan.template.md`
- OpenCode：`/plan-phase` 起草 · `/handoff` 提取执行提示词 · `/accept-phase` 验收

若仓库已使用 `docs/superpowers/plans/`，可继续把计划写在那里，并在元规范/AGENTS 中注明主目录。

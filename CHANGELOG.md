# Changelog

## v1.5.4（2026-08-19）

### 🚀 新功能

- **统一网关端口自定义**：实例池页「统一网关」卡片新增「自定义端口」入口，保存即热生效（`ApplyPort` 热重启，镜像密钥行为）；端口配置走 `env > config > 默认` 三源优先级，重启后不再被 env 固化旧值
- **deb 包自动注册 systemd 服务**：`.deb` 安装后自动注册并启用 `opencode2api` 服务（`systemctl status opencode2api` 查看），默认端口 60000、密钥 `sk-unified-local`，支持 `OPCODE2API_GATEWAY_KEY` env 注入；修改 `/etc/opencode2api/manager.env` 后需 `systemctl daemon-reload && systemctl restart` 才生效
- **Linux/Headless 增强**：`--headless` 无头模式支持（释放内嵌组件 → 拉起 core → 打印管理地址 → 退出自动清理）；Linux 平台二进制解析与可写目录回退（deb/NSIS 安装场景自动复制前端 dist）
- **管理鉴权默认关闭**：裸 core 不再要求 `-password`（默认空 = 不启用登录验证），需要时显式 `-password <密码>`
- **deb 安装完成输出醒目提示**：`postinst` 打印经理.env 路径、重启命令、状态/日志查询命令与管理 WebUI 地址

### 🔧 修复

- **实例池测试误报「无免费模型可测试」**：免费模型名单扩充 `hy3`、`nemotron-3.5-lightning`（上游新增）；`pickFreeModel` 加兜底——本系统 `/v1/models` 已只返回免费模型，data 非空时跳过 auto 虚拟模型取首个可测模型，硬编码名单再过期也不误报
- **网关自定义密码重启失效**：双 config 结构（`AppConfig` / `core/manager.Config`）共用 `config.json` 相互覆盖，改读-合并-写（`MergeConfigJSON`）保留对方字段
- **上游失败响应体透传入调用日志**：日志错误信息完整显示 `upstream status <码>: <原因> body=<上游响应体>`，不再只有 `upstream status 400: <nil>`
- **deb 维护脚本 CRLF 修复**：`.gitattributes` 强制 sh/service/env 文件 LF，修复 dpkg 无法执行 `preinst`（shebang 带 `\r`）的问题

### 🎨 UI/UX

- **七页标题/图标/布局统一**：统一 h1 字号与 teal 图标、去除页面宽度限制（全宽一致）
- **七页按钮尺寸统一至独享页基准**：工具条 `px-3 py-1.5 text-[13px]` / 行内 `px-2.5 py-1 text-[12px]` / 弹窗 `px-4 py-2 text-[13px]`
- **弹窗遮罩防误关**：含表单弹窗点击遮罩不再关闭（含统一网关端口弹窗），防填写中误触丢失输入
- **设置页精简**：移除代理出口区块与退出程序按钮，新增版本标识

### 📦 打包与文档

- CI 增加 `workflow_dispatch` 手动触发；`gitignore` 忽略 linux-out 构建产物与测试图片
- README 同步：deb systemd 服务说明、`apt install ./xxx.deb`、`manager.env` 修改后重启提示、config.json 统一网关端口/密钥说明

### 🙏 致谢

- 感谢 **FYHC1** 贡献 Linux/Headless 支持、deb systemd 部署、统一网关端口/密钥持久化与本版全部 PR（#12–#15），已完成代码审查并合入
- 感谢 **Sujinxin123** / **6Kmfi6HP** 的开源基础与历史贡献（统一网关、免费额度实测健康检查、Go 代理核心）
- 感谢所有反馈与测试的使用者

### 🚀 新功能

- **订阅分组命名增强**：分组名优先取订阅内容里的配置名（`# Profile:` 注释 / 顶层 `profile.name`），其次才按文件名 / URL 末段推导；新增订阅对话框支持手动指定分组名（固定后自动拉取不覆盖）

### 🔧 修复

- **节点扫描 sing-box 启动即崩**：hysteria2 带宽字段（up/down）改为数值 Mbps——字符串会被 sing-box 直接拒绝；vless reality 的 `utls` 移入 `tls` 子对象——原先放 outbound 顶层导致 unknown field 报错
- **探测提示文档化**：FAQ 新增「models 接口连通（无免费模型可测试）」的含义说明与改进方向
- **Linux `--headless -port` 未生效修复**：壳的健康检查/地址打印/窗口导航未对齐透传的 `-port`（core 实际监听指定端口，壳仍探测默认 40000）——现由 `override_port` 解析透传端口并贯穿三处调用点，Debian/Ubuntu 等 Linux 无头模式按实际端口就绪、打印与导航

### 🙏 致谢

- 感谢 **FYHC1** 贡献订阅分组命名与扫描 probe 修复（PR #10 / #11），已完成代码审查并合入

## v1.5.2（2026-08-18）

### 🔧 修复

- **调用日志「清空日志」修复**：运行中的实例/统一网关进程不再直写其日志文件，一律走 HTTP 清空——修复 Windows 下「文件被占用」导致清空失败

### 🧹 仓库整理

- 研发过程文档（MASTER-PLAN / REQ-\* / REVIEW-\* 等）移出仓库：本地保留、gitignore 忽略；仓库内文档聚焦产品契约（API / CONFIGURATION / ROUTING / CUSTOM-MODELS / DEPLOYMENT / FAQ 等）

## v1.5.1（2026-08-17）

### 🔧 重要修复

- **节点扫描（探测）修复**：探测子进程配置不再注入厂商 providers——修复此前所有节点扫描判定失败（可用 0 个）；节点可用性判定改为 `/v1/models` 返回 2xx 即可用（能连通上游就算可用，免费模型 chat 测试仅作额外验证）
- **统计页「重置统计」修复**：运行中实例/统一网关一律走 HTTP 复位，不再直写其统计文件——修复 Windows 下「文件被占用」导致无法重置；统一网关端口按环境槽位生效（env > config > 默认），dev/便携/web-dev 不再打错端口回退直写；实例/网关侧统计落盘增加占用重试与写盘串行化（旧快照不再复活已清零统计）；重置端点鉴权改为与其余管理端点一致的会话鉴权

## v1.5.0（2026-08-17）

### 🚀 新功能：auto 虚拟模型（智能选路，实例池设置弹层配置，默认关闭）

- 客户端只填 `model: "auto"`：网关按「用户权重 × 实测成功率」自动选模型，失败沿候选链无感降级（有界，最多切 3 个）
- 三种策略：均衡（平滑加权轮询，按权重分流额度）/ 速度优先（权重≥5 中选实测最快）/ 能力优先（按权重锁定，失败才降）
- 权重按模型粒度（0~10，0=永不参与，缺省 5，同模型跨实例同权重）；实例（节点）选择仍由 smart/failover 路由负责
- **上下文护栏三道防线**：请求侧保守 token 估算（中/英文自适应，图像不计入）；模型侧有效上限 = min(用户配置, 失败学习值, 保守默认 128k)；整条候选链按 est≤上限×0.9 过滤，全超限按最大上下文兜底
- 失败学习：上游报上下文类错误自动收紧该模型上限（填错真实值会被自动修正）
- 实测反馈滑窗（模型×实例，10 分钟）：auto 与非 auto 流量共同喂养，坏模型自动降权、恢复自动回升，冷启动不惩罚
- 每模型一行同处配置「权重 + 上下文上限」；保存即热生效（子进程 3s 热重载，无需重启）
- 调用日志新增 `auto_pick` 事件：记录选了谁、为什么、降级链是什么
- 三入口全支持：OpenAI Chat / Claude Messages / OpenAI Responses（含流式）

## v1.4.0（2026-08-16）

### 🚀 新功能：自定义模型源（新页「自定义模型」）

- 自带 API key 接入第三方模型供应商，四大协议全支持：OpenAI Chat Completions / Anthropic Messages / OpenAI Responses / Google Gemini
- 多 key 池：轮询 + 错误转移调度，429 自动冷却、401 自动禁用换 key
- 向实例/网关子进程热传播：/v1/models 不重启即生效
- 模型目录磁盘缓存：重启后首个请求即含自定义模型
- 编辑回填已存 key、白名单暴露、源活性探测
- 数据清理与自定义模型解耦，清空缓存不再误伤自定义源

### ⚡ 性能与路由

- 竞速整体预算 + 冷启动不竞速；自适应竞速（按压力动态调整副本数）
- 429 感知：降级单发 + 指数退避 + 可见报错
- 质量自愈分类型恢复：链路类自动自愈 / 账号类（401/402/429）永久禁用
- 上游代理出口配置：绕过本机出口 IP 风控
- 调用日志补齐 + 探活目标可配

### 🛡️ 稳定性（并发审计 27 项 + Code Review 33 项全数修复）

- 请求取消穿透下游、http.Client 复用、统计/日志写盘单写者
- 网关启动互斥、账号借出互斥、订阅每源独立调度
- 熔断半开计时、配置热重载原子化等边界修复

### 🎨 UI/UX

- 全局任务悬浮窗：右下角多任务栈，跨页常驻
- 实例池状态筛选、轮询间隔可配（0=关闭）、日志页分页、统计页迷你条形图
- 停止扫描交互重做（三态+进度）+ 扫描/停止并发配置面板
- 节点池轮询自杀 bug、订阅导入伪节点过滤（与 Clash 对齐）、删除订阅原子化清理

### 🔧 重要修复

- **安装器**：NSIS 保持 perMachine 全用户安装（安装路径与历史版本一致），并在安装后自动授予安装目录写权限——修复此前安装后「127.0.0.1 拒绝连接」的问题，无需先卸载旧版
- 面板请求空 body（清空/保存配置失效）、日志页空日志报错

### 📦 安装包

Windows（NSIS）/ Linux（deb、AppImage）/ macOS（dmg）

## v1.3.3（2026-08-13）

- 保留 Clash/订阅节点 ws Host 头，修复 CDN 节点 403（#8，致谢 383827453-max）
- README 更新（多端部署/订阅管理/性能模式/批量操作/构建流程）与效果图跟踪恢复

## v1.3.2（2026-08-13）

- T1 一键重启仅重启运行中的池成员；T2 实例池批量按钮纯勾选驱动；T3 订阅源列表管理（多条增删拉取+按分组释放进度）；T4 网关密钥设置后立即生效
- V1 订阅解析统一过滤公告/信息伪节点；V2 删除订阅前确认使用中实例数；V3 独享页批量释放；V4+V5 独享页链路质量列/探测开关；直连请求显示「直连」
- sing-box 下载增加重试与镜像兜底

## v1.3.1（2026-08-13）

- Go core 底座迁移收官：订阅/健康巡检/导出/日志过滤/节点删除/网关密钥/headless/前端全部迁移至 /api/admin HTTP
- 端口槽位表（40000/44100/48200/52300）+ 实例/sing-box 端口避让；统一网关端口 18080→40080
- Linux/macOS/Windows 三平台打包矩阵（deb/appimage/dmg/nsis）+ WebView2Loader.dll 纳入仓库
- 性能模式（链路探活/质量加权/熔断半开）、请求级竞速（P2b）、残留进程探测与一键清除
- UI：设置页卡片归位/折叠面板、路由模式分三行、退出二次确认、并发设置可配

## v1.3.0（2026-08-09）

### 🎉 特别致谢

- **感谢 [FYHC1](https://github.com/FYHC1)**：v1.3.0 的核心贡献者——完整实现了 **Linux / Headless 支持**（`serve` 子命令、systemd 服务、跨 WSL/Windows 构建）、**浏览器 WebUI**（桌面与 headless 共用 axum HTTP 层）、**前端嵌入 exe**（单文件自包含，无需 dist 伴行）、**订阅拉取与批量导入**、**健康巡检自动重启**、**报表导出**、**配置化**（网关端口/密钥、HTTP 端口）等一系列能力，是本版本 Linux/WebUI 架构的基石。
- **感谢 [Sujinxin123](https://github.com/Sujinxin123)**：v1.0.0「统一网关、免费额度实测健康检查、代理池健康检查、配置热更新、模型必填、同模型重试、批量并行启停」等核心能力均移植自该项目。
- **感谢 [6Kmfi6HP](https://github.com/6Kmfi6HP)**：Go 代理核心（OpenAI/Anthropic/Responses 协议转换）的开源贡献。
- 感谢所有贡献者与使用者的反馈与支持。

### 🚀 新功能（Linux / Headless）

- **Linux 桌面**：支持 .deb / AppImage 安装包
- **Headless 模式**：`opencode2api serve --port 19090` 纯浏览器管理，systemd 一键部署
- **WebUI**：桌面与 headless 共用本地 HTTP 服务（`127.0.0.1:19090`），浏览器可完整管理
- **前端嵌入 exe**：单文件自包含，无需磁盘 `dist/` 伴行
- **订阅拉取与批量导入**（Clash YAML / base64 / v2ray），支持后台自动拉取
- **健康巡检**：周期 TCP 探测 + 失败自动重启 + `health.json` 持久化

### 🚀 新功能（通用）

- **节点池重设计**：扫描结果弹窗一键「入实例池 / 设为独享」，自动去重
- **并行扫描提速**（默认 8 并发，可调）
- **日志分析**：时段分析、节点分析视图（纯 CSS 条形图）、按天筛选、一键清空
- **重置 Token 统计**（二次确认，可清除已删除节点历史）
- **报表导出**：日志 CSV / 实例 JSON / 统计 JSON
- **配置化**：`config.json` 支持网关端口/密钥（热生效）、HTTP 端口、订阅、巡检、日志过滤
- **自定义统一网关密钥**（校验 + 热生效 + 前端更换交互）

### 优化

- 架构重构：`AppCore` 纯逻辑核心，桌面 command 与 headless HTTP 完全同源
- 修复 release 版误加载 devUrl 导致 localhost 拒绝连接的问题
- 非 Windows 平台 autostart 修复

## v1.2.0（2026-08-08）

### 新增

- 节点池「扫描 → 入实例池/设为独享」弹窗一键操作，自动去重
- 并行扫描提速（默认 8 并发）
- 日志页分析视图：时段分析、节点分析（纯 CSS，轻量）
- 重置 Token 统计（二次确认，可清除已删除节点）
- 日志按天筛选、一键清空
- 独享/实例池测试结果行内徽章，批量测试并行执行

### 优化

- 节点池：组全选跳过已实例化、入池后自动清勾选、徽章区分实例池/独享、选中行高亮
- 凭据仅无凭据时显示；重置弹窗文案简化

### 修复

- 修复节点池入池后勾选态残留无法取消
- FAQ 补充 400 报错日志菜单显示；README 增补轻量化原则

## v1.1.0（2026-08-06）

### ✨ 智能路由（默认开启）

- **smart 默认路由模式**：在故障转移基础上叠加健康计数——节点故障自动计数，连续 3 次自动停用该节点并切换，正常轮次自动清零，全程无需手动干预
- **坏状态码自动切换**：遇到 401/402/429/503 自动切换下一个健康节点
- **流内超时自动续写**：首字等待超时（10s）或回复静默（5s）自动切换节点并断点续写，对话不中断；长任务不受全局超时限制

### 🆕 新增功能

- **全流程调用日志**：成功/失败/中断均完整记录，日志页可按天查看
- **清除数据**：设置页一键清理（运行数据 / 实例记录 / 全部重置，全量重置自动备份配置），解决环境残留异常
- **一键重启实例池**：全停 → 强制释放被占端口 → 全部重启 → 网关同步，端口占用一键解决
- **配置即时生效**：修改路由/超时等配置自动生效，无需手动重启

### 🐛 修复与优化

- **SSE 流式兼容性**：事件按标准空行分隔，修复 IDE 等严格客户端解析失败
- **token 统计修正**：流式响应只记录最终用量，不再逐 chunk 累加
- **退出彻底清理**：退出时自动停止统一网关与全部实例（含残留进程），端口不再后台常驻
- **日志页修复**：修复日志页无数据问题

## v1.0.2（2026-08-05）

### 🐛 Bug 修复

- **sing-box 生成配置修复**：生成配置不再包含非法 `transport {type:tcp}`，修复扫描时 sing-box 启动即退出的问题（FIX_LOG 3.1）（贡献：FYHC1）
- **统一网关 token 统计修复**：网关进程未设置工作目录导致 token 统计错位落盘应用 exe 目录，统计界面读不到网关流量；现已对齐到 `runtime/_unified-gateway/`（贡献：FYHC1）
- **统一网关按节点拆分 token 统计**：界面新增「调用节点明细」，按 SOCKS5 出口节点聚合显示各节点请求数与 token 用量；直连实例不再产生无效的 `node_stats.json`（贡献：FYHC1）

### 🙏 致谢

感谢 [FYHC1](https://github.com/FYHC1) 对本版本的两项 Bug 修复贡献（#2 sing-box 配置、#3 统一网关 token 统计）。



## v1.0.1（2026-08-04）

### 🐛 Bug 修复

- **实例池全部启动/停止卡顿**：改为复用 Rust 并行命令（batch_start 4 worker / batch_stop 8 worker），不再前端逐个串行等待
- **节点池重复添加**：批量添加按 node 去重，同一节点只允许例化一次（后端拒绝 + 前端过滤已添加节点）
- **实例池统计口径**：「池内 x / 共 y」中的「共 y」只统计池成员，不再混入独享实例



## v1.0.0（2026-08-04）

### 亮点

首个正式版：在 A 多实例管理的基础上，融合 B 的 7 项核心能力，形成「独享实例 + 统一实例池」双容器管理模型。

### 🆕 新增功能

- **统一网关**：实例池聚合入口 `127.0.0.1:40080`，failover / round_robin 两种路由模式，一键启停
- **免费额度实测健康检查**：启动即探测免费模型可用性（名称含 `-free` 或为 `big-pickle`）
- **配置热更新**：修改运行配置不重启进程、不打断进行中的 SSE 流
- **模型必填校验**：chat / claude / responses 缺 model 一律返回 400
- **同模型失败重试**：同一模型路由失败自动重试；代理池健康检查 + 坏代理自动冷却
- **批量并行启停**：多 worker 并行，批量操作大幅提速；节点扫描并行探测

### 🎨 界面与体验

- 三页边界清晰：**独享**（仅独享实例启停/测试/释放）/ **实例池**（池成员启停、一键测试、释放、网关管理）/ **节点池**（扫描 + 独享或进池批量添加）
- 搜索、状态筛选、一键测试、密钥一键复制（API 地址 + 密钥合并复制）
- 移除即释放：自动关闭实例并释放节点，无中间态

### 🛠 其他

- GitHub Actions 自动构建 Windows NSIS 安装包并发布 Release
- 完整合并交付文档（见 docs/ 与 .tokeny/plans/）

### 🙏 致谢

感谢 [Sujinxin123](https://github.com/Sujinxin123)——本版本「统一网关、免费额度实测健康检查、代理池健康检查、配置热更新、模型必填、同模型重试、批量并行启停」等核心能力均移植自该项目，是本次合并的基石。

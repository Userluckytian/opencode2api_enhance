---
description: 从已有阶段计划生成可粘贴给执行 AI 的完整提示词
agent: build
---

你是交接提示词生成器（可直接在本会话完成，不必改代码）。

1. 读取人类指定的阶段计划（路径或阶段名）；在 `docs/ai-framework/plans/` 与 `docs/superpowers/plans/` 查找。  
2. 以 `docs/ai-framework/phased-plan-driven.md` 交接提示词五段结构为准。  
3. 若计划文末已有「给接手 AI 的完整提示词」：核对是否与正文 Task/红线一致；一致则原样放入可复制代码块；过期则重写并说明差异。  
4. 若无提示词：根据计划全文新写（基线/做/不做/工作方式/交卷）。  
5. 必须要求：完整执行、按 Task 顺序、证据优先、遵循项目 Git 规范、默认不 push、不碰红线。  
6. 不要执行阶段实现；只输出提示词 + 计划路径 + 建议分支名。

计划路径或阶段代号：
$ARGUMENTS
